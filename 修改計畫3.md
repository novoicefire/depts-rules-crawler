目前 parser 主要問題已修正，請只做以下小修：

1. src/ncnu_graduation/validator.py

目前 group schema 必要欄位只有：
groupId, name, type, courses, originalRows

請把 rules 加進必要欄位：

["groupId", "name", "type", "courses", "rules", "originalRows"]

並新增 rules 型別檢查：

if not isinstance(g.get("rules"), list):
report["invalid_schema"].append({
"id": req_id,
"error": f"group {idx} rules must be a list"
})

2. scripts/run_probe.py

目前 validate command 是：

python cli.py validate --probe

請改成：

python cli.py validate --probe --strict

3. src/ncnu_graduation/parser.py

如果 is_reasonable_title() 已經沒有被使用，請刪除該 function。
不要改 find_group_name() 的核心邏輯。

4. 不要修改 fetch。
5. 不要修改 Playwright。
6. 不要改 GitHub Action。
